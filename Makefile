  # Makefile for meli-backend

  install:
		npm install

  start:
		npm start

  dev:
		npm run dev

  test:
		npm test

  docker-build:
		docker-compose build

  docker-up:
		docker-compose up

  docker-down:
		docker-compose down
